const { } = require('../frontend.async/logout.automation.async')

jest.setTimeout(30000); // Set a higher timeout for Selenium tests

describe('Testing Account Logout Frontend with Jest and Selenium-Webdriver', () => {
    test('', async () => {
        
    });
});